# Streaming Basic

This fixture should be streamed in small chunks.

The renderer should keep paragraphs stable while **bold text is still open and then closes**.

Another sentence contains *italic text that spans chunks* and ~~strikethrough text~~.

Special characters should stay visible: `<`, `>`, `&`, `$`, `` ` ``.
