// swift-tools-version: 5.9
import PackageDescription

// Generated by publish_to_public.sh. Edit the template there, not this file.
let package = Package(
    name: "swift-markdown-kit",
    platforms: [
        .iOS(.v17)
    ],
    products: [
        .library(
            name: "SwiftMarkdownKit",
            targets: ["SwiftMarkdownKit"]
        )
    ],
    targets: [
        // The framework is downloaded from this tag's release rather than
        // committed. A binary in git is permanent, and a package that carries
        // one makes every consumer clone every version ever published.
        //
        // The URL is immutable: a release asset cannot be replaced under a tag
        // that already exists, so this checksum stays correct for as long as
        // v0.0.9 does.
        .binaryTarget(
            name: "SwiftMarkdownKit",
            url: "https://github.com/cellgit/swift-markdown-kit/releases/download/v0.0.9/SwiftMarkdownKit-0.0.9.xcframework.zip",
            checksum: "78fe21e3ed14945c527787bcbeb45d9c98cc5b282b446b1d6bcd5b211a13edfd"
        )
    ]
)
